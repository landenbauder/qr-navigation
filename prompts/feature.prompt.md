---
description: Implement a scoped feature with minimal risk and built-in verification.
agent: agent
---

# Feature implementation request

You are working on this repository:
- Static site with vanilla JavaScript (`app.js`), HTML (`index.html`), CSS (`styles.css`), and office data (`offices.json`).

Task:
{{input:Describe the feature to implement in one sentence}}

Constraints:
- Keep changes minimal and focused.
- Do not introduce frameworks or build tooling.
- Preserve existing UX unless explicitly requested.
- Maintain compatibility with `offices.json` and current map/routing/geolocation behavior.

Execution steps:
1. Explore current implementation and impacted files.
2. Propose a short plan.
3. Implement the feature.
4. Validate with targeted checks.
5. Update docs only if behavior changed.

Acceptance criteria:
{{input:List acceptance criteria and expected results}}

Validation instructions:
- Provide a quick manual test checklist.
- If there are runnable checks, run them and report output.

Output format:
- Summary of changes
- Files changed
- Validation performed
- Risks or follow-ups
